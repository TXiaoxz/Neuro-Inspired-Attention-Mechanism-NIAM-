# ============================================
# Hardware Configuration:
# CPU: AMD Ryzen 9 9950X3D
# GPU: NVIDIA RTX 5090 (32GB VRAM)
# RAM: 64GB
# CUDA: 13.0
# ============================================

# Deep Learning Framework (CUDA 13.0 compatible)
# Install PyTorch with CUDA 13.0 support:
# pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
torch>=2.5.0
torchaudio>=2.5.0
torchvision>=0.20.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
audiomentations>=0.30.0
pydub>=0.25.0
resampy>=0.4.2

# Data Loading & Processing
datasets>=2.14.0  # Hugging Face datasets
transformers>=4.30.0
numpy>=1.24.0
scipy>=1.10.0

# Audio Decoding
ffmpeg-python>=0.2.0

# Evaluation Metrics
jiwer>=3.0.0  # Word Error Rate calculation

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
tensorboard>=2.13.0

# Real-time Audio
# PyAudio might need system dependencies: sudo apt-get install portaudio19-dev
# pyaudio>=0.2.13  # Commented out - requires portaudio system dependency
sounddevice>=0.4.6

# Configuration
pyyaml>=6.0
omegaconf>=2.3.0

# Utilities
tqdm>=4.65.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Jupyter
jupyter>=1.0.0
ipywidgets>=8.0.0
notebook>=7.0.0

# Performance Optimization (for RTX 5090)
# Enables faster training with mixed precision, distributed training
accelerate>=0.20.0
# deepspeed>=0.9.0  # Uncomment if needed for large-scale training

# Memory optimization
psutil>=5.9.0

# Additional utilities
einops>=0.7.0  # Tensor operations
timm>=0.9.0    # Vision models (if needed)
